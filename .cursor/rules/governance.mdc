---
description: Governance rules for mermaid-monorepo — quality gates, security, conventions
globs:
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
alwaysApply: true
---

# Governance — mermaid-monorepo

> Generated from governance.md by crag. Regenerate: `crag compile --target cursor`

**Stack:** node, typescript, docker

## Quality Gates

Run these checks in order before committing:
### Lint
- `npm run lint`
- `npx tsc --noEmit`

### Test
- `npm run test`

### Build
- `npm run build`

### Ci (inferred from workflow)
- `pnpm run --filter mermaid types:build-config`
- `pnpm run docs:build`
- `pnpm run build:viz`
- `ERROR_MESSAGE+=' make sure your packages are up-to-date by running `pnpm install`.'`
- `ERROR_MESSAGE+=' You may also need to delete your prettier cache by running'`
- `ERROR_MESSAGE+=' `rm ./node_modules/.cache/prettier/.prettier-cache`.'`
- `ERROR_MESSAGE+=' `pnpm run --filter mermaid types:build-config`'`
- `ERROR_MESSAGE+=' Run `pnpm run --filter mermaid checkCircle` on your local machine'`
## Security

- No hardcoded secrets — grep for sk_live, AKIA, password= before commit

## Conventions

- Follow project commit conventions
- Runtimes: node
