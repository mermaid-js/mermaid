import { registerIconShapeTests } from '../helpers/iconShapeTests.ts';

// `circle` form slice of the iconShape matrix. See iconShapeTests.ts and
// iconShape.spec.ts for why this matrix is split across files.
registerIconShapeTests(['circle']);
