describe('just an example', function () {
  it('example', function () {
    expect(true).to.equal(true);
  });

  it('example2', function () {
    expect(true).to.equal(true);
  });

  it('example3', function () {
    expect(true).to.equal(true);
  });
});
