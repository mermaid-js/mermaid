%% §12 — tool requires a capability not in the executing agent's
%% permits.
agentflow TB
  agent researcher["R"]
    caller["c"]
    t["t"]
    t@{ shape: subroutine, requires: ["net.write"] }
    caller --> t
  end
  researcher@{ permits: ["net.read"] }
