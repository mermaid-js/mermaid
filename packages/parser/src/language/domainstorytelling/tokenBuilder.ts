import { AbstractMermaidTokenBuilder } from '../common/index.js';

export class DomainStorytellingTokenBuilder extends AbstractMermaidTokenBuilder {
  public constructor() {
    super(['domainstorytelling-beta']);
  }
}
