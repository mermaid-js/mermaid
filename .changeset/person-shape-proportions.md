---
'mermaid': patch
---

fix: give the person shape a body at least 0.6 times as tall as it is wide, let its head grow to a
radius of 80, and centre the label on the part of the body below the head, so the figure no longer
reads as a flat bar with a small head when the label is short.
