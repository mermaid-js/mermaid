---
'mermaid': minor
---

feat(c4): support the `direction` statement (TB/BT/LR/RL) in the C4 unified renderer, so opt-in unified diagrams can control layout orientation. The legacy renderer is unaffected.
