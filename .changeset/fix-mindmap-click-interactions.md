---
'mermaid': minor
---

Add click interactions to MindMap diagrams

MindMap nodes can now be made clickable with `click <nodeId> href "<url>"` syntax, supporting optional link targets (`_blank`, `_self`, etc.) and tooltips. This brings MindMap into feature parity with other Mermaid diagram types that support click interactions.
