---
'mermaid': major
---

fix: change default fonts to use open-source WebFonts

The `neo` themes now prefer Arimo over Arial.
`Open Sans Variable` is now preferred over `Open Sans` by default.
Classic themes prefer `Atkinson Hyperlegible Next` over `Trebuchet MS`.
