---
'mermaid': minor
'@mermaid-js/parser': minor
---

feat(radar): add range-valued curve entries

Radar curve entries can use `[start..end]` to fill between independently defined inner and outer
values on each axis. Scalar values continue to fill from the diagram minimum.
