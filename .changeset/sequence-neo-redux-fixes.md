---
'mermaid': patch
---

fix: sequence diagrams under the `neo` look and `redux` themes — notes no longer render bold, the
stick-figure actor is drawn full size with its label on the same baseline as the other participant
shapes, and a `rect` section band is no longer drawn white on white.
