---
'@mermaid-js/syntax-metadata': minor
---

Add `@mermaid-js/syntax-metadata`, an experimental package exposing diagram syntax metadata (keywords, operators, lexer contexts) extracted from mermaid's own jison and Langium grammars, plus curated snippets, docs links, declaration skeletons and identifier helpers for editors.
