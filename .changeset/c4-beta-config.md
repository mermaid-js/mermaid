---
'mermaid': minor
---

feat(c4-beta): add a `c4beta` config section with `useMaxWidth` (default `true`) and `diagramPadding` (default `10`), wired into the c4-beta renderer and overridable via frontmatter config.
