---
'mermaid': minor
---

Add experimental threat-model annotations to ordinary flowcharts via versioned YAML frontmatter. Validate model references and mitigation decisions, return structured metadata from parse/render, and display threat badges, trust boundaries, and a threat register. Include a local editing demo and documentation.
